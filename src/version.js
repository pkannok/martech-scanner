const { version } = require('../package.json');

const SCANNER_NAME = 'MarTech Scanner';
const SCANNER_VERSION = version;
const REPORT_TEMPLATE_VERSION = '2.6';

module.exports = { SCANNER_NAME, SCANNER_VERSION, REPORT_TEMPLATE_VERSION };
